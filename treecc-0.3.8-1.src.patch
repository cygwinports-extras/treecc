diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4*' -x ltmain.sh -x 'config.*' -x depcomp -x install-sh -x missing -x mkinstalldirs -x autom4te.cache -x '*compile' -x Makefile.in.in -x 'intltool*.in' -x 'xml-i18n-*.in' -x '*.pyc' -x '*.mo' -x '*.gmo' -x ABOUT-NLS -x Makevars.template -x COPYING -x INSTALL -x '*.orig' -x '*.rej' -x '*~' -x '*.temp' -x texinfo.tex -x ylwrap -x Makefile.in -x makefile.in -x configure -x gnome-doc-utils.make -x intltool.m4 -x omf.make -x xmldocs.make origsrc/treecc-0.3.8/configure.in src/treecc-0.3.8/configure.in
--- origsrc/treecc-0.3.8/configure.in	2006-01-14 23:11:41.000000000 -0600
+++ src/treecc-0.3.8/configure.in	2006-08-10 12:17:43.468500000 -0500
@@ -8,10 +8,6 @@
 dnl Turn off the cygwin library if building for Win32.
 AC_MSG_CHECKING([if building for some Win32 platform])
 case "$host" in
-	*-*-mingw*|*-*-cygwin*)
-		platform_win32=yes
-		CC="gcc -mno-cygwin"
-		;;
 	*-*-mingw*)
 		platform_win32=yes
 		;;
